require "spec_helper"

describe ShakespeareAnalyzer do
  it "has a version number" do
    expect(ShakespeareAnalyzer::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
